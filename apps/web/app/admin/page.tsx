'use client';

import Link from 'next/link';
import { useEffect, useState } from 'react';
import { createClient } from '@/lib/supabase/client';

const modules=[['Admissions','Candidatures et dossiers','/admin/admissions'],['Étudiants','Dossiers et inscriptions','/admin/students'],['Structure académique','Filières, classes et matières','/admin/academic'],['Enseignants','Affectations et matières','/admin/teachers'],['Notes & examens','Saisie, validation et bulletins','/admin/grades'],['Finance','Frais, paiements et impayés','/admin/finance'],['Documents','Attestations, bulletins et reçus','/admin/documents'],['Utilisateurs','Rôles et permissions','/admin/users'],['Journal d’activité','Historique des actions','/admin/activity'],['Configuration','Établissement et année académique','/admin/settings']];
export default function AdminHome(){const supabase=createClient();const [stats,setStats]=useState({students:0,applications:0,teachers:0,programs:0});useEffect(()=>{(async()=>{const [{count:students},{count:applications},{count:teachers},{count:programs}]=await Promise.all([supabase.from('students').select('*',{count:'exact',head:true}),supabase.from('admission_applications').select('*',{count:'exact',head:true}),supabase.from('teachers').select('*',{count:'exact',head:true}),supabase.from('programs').select('*',{count:'exact',head:true})]);setStats({students:students??0,applications:applications??0,teachers:teachers??0,programs:programs??0})})()},[]);return <main className="min-h-screen bg-slate-50 text-slate-900"><div className="mx-auto max-w-7xl px-5 py-8"><header className="flex flex-wrap items-center justify-between gap-4"><div><p className="text-sm font-black tracking-[.25em] text-blue-600">CONIK</p><h1 className="mt-1 text-2xl font-bold">Administration</h1><p className="text-sm text-slate-500">Centre de contrôle de votre établissement</p></div><Link href="/" className="rounded-xl border bg-white px-4 py-2 text-sm font-semibold shadow-sm">Accueil</Link></header><section className="mt-8 grid gap-4 sm:grid-cols-2 lg:grid-cols-4">{[['Étudiants',stats.students],['Candidatures',stats.applications],['Enseignants',stats.teachers],['Filières',stats.programs]].map(([label,value])=><div key={String(label)} className="rounded-2xl border bg-white p-5 shadow-sm"><p className="text-sm text-slate-500">{label}</p><p className="mt-2 text-3xl font-black">{String(value)}</p></div>)}</section><section className="mt-10"><div className="flex items-end justify-between"><div><h2 className="text-xl font-bold">Modules</h2><p className="mt-1 text-sm text-slate-500">Construisons chaque module avec données réelles, actions et contrôles.</p></div></div><div className="mt-5 grid gap-4 sm:grid-cols-2 lg:grid-cols-3">{modules.map(([title,desc,href])=><Link key={href} href={href} className="group rounded-2xl border bg-white p-5 shadow-sm transition hover:-translate-y-0.5 hover:shadow-md"><div className="flex items-start justify-between"><div><h3 className="font-semibold group-hover:text-blue-600">{title}</h3><p className="mt-2 text-sm text-slate-500">{desc}</p></div><span className="text-slate-400">→</span></div></Link>)}</div></section></div></main>}
